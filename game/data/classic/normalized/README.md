# Normalized Classic Data

This folder contains runtime-facing JSON generated from the original text sources.

Rules:

- regenerate this data from importer scripts instead of editing it by hand
- keep the raw importer output for traceability
- document intentional deviations from original balance elsewhere
