# Classic Data

This folder holds imported and normalized data derived from the original game.

Rules:

- `raw/` keeps importer output close to the original source files
- `normalized/` is the runtime-facing export used by the game code
- normalized formats should remain traceable back to the original source
- do not mix new Rising Lands 2 content into this folder
